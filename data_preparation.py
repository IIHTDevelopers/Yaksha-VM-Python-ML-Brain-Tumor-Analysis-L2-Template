import os
import numpy as np
import tensorflow as tf
from sklearn.model_selection import train_test_split

def load_data(data_dir):
    # Skeleton function returning None
    return None, None

def prepare_data(data_dir):
    # Skeleton function returning None to simulate failure
    return None
